# F2.E - Ownership map / docs

Fecha: 2026-04-23

## Objetivo

Alinear los documentos normativos con el estado real despues de F1 closeout, F1 patterns de dominio y F2.A-F2.D.

Esta subfase no cambia codigo. Solo corrige el contexto que futuros agentes van a leer antes de tocar el proyecto.

## Problema detectado

`docs/rebuild/02-legacy-new-ownership-map.md` seguia congelado al cierre de F0 y marcaba como `legacy` superficies que la auditoria posterior ya habia confirmado como migradas visualmente:

- `AuthView.jsx`
- `SurveyView.jsx`
- `CoursesView.jsx`
- `LessonView.jsx`

La evidencia relevante:

- F0.6 reporto 0 clases legacy en esas cuatro vistas.
- F0.8 confirmo que las vistas migradas no dependen de clases legacy en JSX.
- F0.9 pidio actualizar el ownership map para reflejar esa evidencia.
- F2.A-F2.D cerraron la parte tecnica de shell closeout que tambien estaba desactualizada en `CLAUDE.md` y `AGENTS.md`.

## Cambios realizados

### 1. Ownership map actualizado

Archivo:

- `docs/rebuild/02-legacy-new-ownership-map.md`

Cambios:

- Se reemplazo el mapa de F0 por un mapa actualizado a F2.E.
- Se aclaro la diferencia entre `legacy`, `transition`, `new-ui` y `frozen-contract`.
- Se marco `AuthView`, `SurveyView`, `CoursesView` y `LessonView` como `new-ui`, con deuda de refinamiento asignada a sus fases reales.
- Se marco `App.jsx` y `useResponsiveLayout.js` como `transition`.
- Se marco `DeviceShell`, shells, policies, navigation y route-containers como `new-ui`.
- Se conservaron `ActivityRenderer.jsx` y `activityRegistry.js` como `frozen-contract`.
- Se mantuvieron actividades legacy, simulaciones, ChatDrawer/Admin, `app.css` y `legacy.css` fuera de alcance.

### 2. CLAUDE.md actualizado

Archivo:

- `CLAUDE.md`

Cambios:

- La fase actual ya no dice que F2 esta pendiente.
- Se registra que F2 fue abierta como closeout tecnico.
- Se listan F2.A-F2.E como cerradas localmente.
- Se deja F2.F como siguiente paso recomendado.
- Se mantiene la regla de no abrir F3-F7 sin autorizacion explicita.

### 3. AGENTS.md actualizado

Archivo:

- `AGENTS.md`

Cambios:

- La seccion de estado actual pasa de F1-only a estado despues de F2.E.
- Se registra que F2.A-F2.E quedaron cerradas localmente.
- Se conserva el warning de no retomar WIP de simulaciones.
- Se mantiene `F2.F - F2 closeout validation` como siguiente bloque.

## Fuera de alcance respetado

- No se toco codigo de frontend.
- No se tocaron vistas grandes.
- No se tocaron simulaciones.
- No se toco dominio.
- No se toco backend.
- No se tocaron services, DB, scoring ni contracts API.
- No se tocaron hooks de dominio.
- No se toco `ActivityRenderer.jsx`.
- No se toco `activityRegistry.js`.
- No se hizo cleanup de `app.css` / `legacy.css`.
- No se abrio F3.

## Validacion

- `git diff --check` - PASS, sin errores de whitespace.
- Validacion funcional/build no requerida para F2.E porque solo modifica documentacion.

## Veredicto

F2.E queda cerrado. Los documentos normativos ya no arrastran el estado aparente de F0/F1 y ahora reflejan que F2 esta en closeout, con F2.F como siguiente bloque antes de pedir autorizacion para F3.
