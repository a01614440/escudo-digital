# F6.F - SMS refine closeout

Fecha: 2026-04-23

## Estado

F6.F quedo cerrada con una rama SMS propia dentro de `InboxActivity.jsx`.

## Que cambie

- `frontend/src/components/activities/immersive/InboxActivity.jsx`
  - active una rama SMS dedicada con clases `email-sim inbox-sim-sms sms-summary-shell`.
  - use la skin existente de `sms-*` para topbar, header, bandeja, lector, mensajes y acciones.
  - mantuve la rama de correo separada con `email-sim inbox-sim-mail`.
  - conserve la logica de clasificacion y completion sin tocar scoring ni dominio.
- `test/f6-sms-refine-smoke.test.js`
  - agregue guards de fuente para fijar la rama SMS y sus selectores de estilo.

## Validacion

- `.\npm-local.cmd test` -> 109/109 pasando.
- `.\npm-local.cmd run build` -> pasando.
- `git diff --check` -> solo warnings de LF/CRLF en los archivos tocados.

## Fuera de alcance

No se toco `ActivityRenderer.jsx`, `activityRegistry.js`, hooks de dominio, backend, services, contracts, scoring, `CoursesView.jsx`, `LessonView.jsx`, `app.css` ni `legacy.css`.

## Siguiente paso

`F6.G` - Inbox / email refine.
