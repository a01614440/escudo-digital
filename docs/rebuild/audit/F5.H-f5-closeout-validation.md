# F5.H - F5 closeout validation

Fecha: 2026-04-23
Commit base al validar: `03f4f78` (`refactor(F5.G): tune route lesson comfort`)

## Objetivo

Cerrar F5 expandida como fase de experiencia real `route -> lesson -> practice`, sin implementar cambios nuevos.

F5.H valida que las subfases F5.A-F5.G dejaron estable:

- la pantalla de ruta / continuidad;
- la transicion ruta -> lesson;
- el lesson shell;
- el activity chrome comun;
- el feedback;
- el confort responsive y la sensacion content-first.

## Validacion ejecutada

- `.\npm-local.cmd test` - 91/91
- `.\npm-local.cmd run build` - verde
- `.\npm-local.cmd run build-storybook` - verde
- `git diff --check` - limpio

Notas:

- Storybook mantiene el warning conocido de chunks mayores a 500 kB.
- No hubo errores de compilacion, test ni whitespace.

## Evidencia adicional de cierre

Line counts al cierre:

- `frontend/src/components/CoursesView.jsx` - 1066 lineas
- `frontend/src/components/LessonView.jsx` - 796 lineas
- `frontend/src/components/activities/sharedActivityUi.jsx` - 157 lineas
- `frontend/src/components/FeedbackPanel.jsx` - 142 lineas

Busqueda de senales prohibidas en superficies F5 cerradas:

- sin coincidencias para `legacy`
- sin coincidencias para `WorkspaceLayout`
- sin coincidencias para `activityRegistry`
- sin coincidencias para `useSimulationEngine`

## Subfases cerradas

### F5.A - Experience baseline audit

Documento:

- `docs/rebuild/audit/F5.A-experience-baseline-audit.md`

Cierre:

- audito ruta, transicion, lesson y activity chrome como una sola familia de experiencia;
- confirmo problemas reales de fragmentacion, columnas rigidas, poco ancho util y baja sensacion fullscreen;
- no implemento cambios.

### F5.B - Route top / continuity / CTA refactor

Documento:

- `docs/rebuild/audit/F5.B-route-top-continuity-cta.md`

Cierre:

- unifico el top de ruta en `RouteBriefing`;
- dejo CTA dominante con `data-sd-primary-cta="courses-continuity"`;
- redujo bloques superiores fragmentados.

### F5.C - Route density / symmetry / contrast cleanup

Documento:

- `docs/rebuild/audit/F5.C-route-density-symmetry-contrast.md`

Cierre:

- aplano la pestana Ruta a dos paneles;
- elimino `RouteInsightRail`;
- compacto `RouteModulePill`, `ModuleActivityList`, `ModuleMissionBoard`, `ProgressScene` y `SettingsScene`.

### F5.D - Route-to-lesson transition pass

Documento:

- `docs/rebuild/audit/F5.D-route-to-lesson-transition.md`

Cierre:

- alineo `LessonMissionHero` con el lenguaje visual de `RouteBriefing`;
- agrego continuidad con `data-sd-lesson-source="courses-continuity"`;
- dejo animacion `sd-lesson-enter`.

### F5.E - Lesson shell layout refactor

Documento:

- `docs/rebuild/audit/F5.E-lesson-shell-fullscreen.md`

Cierre:

- retiro `WorkspaceLayout` del render principal de `LessonView`;
- agrego modos `guided` / `immersive`;
- hizo que actividades inmersivas rendericen sin panel de lesson anidado;
- extendio `ActivityChrome` para bypass inmersivo comun.

### F5.F - Activity chrome + instructions + feedback integration

Documento:

- `docs/rebuild/audit/F5.F-activity-chrome-feedback.md`

Cierre:

- elimino briefing duplicado entre `LessonActivityStage` y `ActivityChrome`;
- hizo `SimulationGuide` colapsable;
- compacto `FeedbackPanel` en una sola superficie.

### F5.G - Comfort / jugabilidad / responsiveness pass

Documento:

- `docs/rebuild/audit/F5.G-comfort-jugabilidad-responsiveness.md`

Cierre:

- balanceo proporciones de ruta;
- hizo secundario el `JourneyStepper`;
- compacto el stage guiado;
- hizo secundario/collapsable el mapa del modulo;
- reforzo presencia fullscreen de inmersivas desde el contenedor comun.

## Guardrails activos

Tests relevantes al cierre:

- `test/f4-courses-dashboard-smoke.test.js`
- `test/f5-lesson-transition-smoke.test.js`
- `test/f5-lesson-shell-smoke.test.js`
- `test/f5-activity-chrome-smoke.test.js`
- `test/f5-comfort-responsive-smoke.test.js`

Protegen:

- CTA y continuidad de ruta;
- dos paneles en ruta, sin insight column permanente;
- transicion visual ruta -> lesson;
- layout `guided` / `immersive`;
- bypass de `ActivityChrome` para inmersivas;
- briefing unico y colapsable;
- feedback compacto;
- comfort responsive final.

## Limites respetados

F5 expandida no toco:

- `ActivityRenderer.jsx`;
- `activityRegistry.js`;
- `useSimulationEngine.js`;
- hooks de dominio;
- services;
- backend;
- contracts;
- scoring;
- internals especificos de simulaciones;
- `app.css`;
- `legacy.css`.

## Deuda viva fuera de F5

No bloquea el cierre de F5, pero debe quedar visible:

- simulaciones especificas siguen para F6A-F6F;
- WhatsApp / chat tiene base mejor envuelta, pero sus detalles de canal son F6A;
- SMS, Inbox, ScenarioFlow, CallSimulation y WebLab requieren auditoria propia por subfase;
- Chat global y Admin siguen para F7;
- cleanup de `app.css` / `legacy.css` sigue para F8;
- Storybook mantiene warning de chunk grande.

## Cierre binario

F5 expandida queda cerrada localmente.

## Siguiente frente recomendado

Abrir F6.A - WhatsApp / chat baseline audit.

Regla: F6.A debe iniciar con auditoria, no implementacion directa.
