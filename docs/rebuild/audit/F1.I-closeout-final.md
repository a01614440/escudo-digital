# F1.I - Closeout final de foundation

Fecha: 2026-04-23

## Fuente de verdad usada

- `docs/rebuild/audit/F0.9-synthesis-and-next-steps.md`
- Commits locales F1.A-F1.H
- `test/foundation-smoke.test.js`
- `frontend/src/design-system/tokens.js`
- `frontend/src/styles/tailwind.css`
- `frontend/src/components/ui/*`
- `frontend/src/patterns/*`

## Alcance cerrado en F1 closeout

| Bloque | Estado | Evidencia |
|---|---|---|
| F1.A Typography tokens | Cerrado | `typographyTokens` existe en `tokens.js`; las clases `.sd-title-*`, `.sd-heading-*`, `.sd-copy`, `.sd-eyebrow` consumen variables tipograficas en `tailwind.css`; smoke tests cubren tokens y CSS. |
| F1.B Educational tokens | Cerrado | `educationalTokens` y `colorTokens.educational` existen en `tokens.js`; variables CSS `--sd-evidence`, `--sd-safe-action`, `--sd-coach`, `--sd-simulation-*`, `--sd-progress-*` existen en `tailwind.css`; smoke tests cubren mirrors. |
| F1.C InlineMessage a11y | Cerrado | `InlineMessage.jsx` mapea tono a `role` y `aria-live`; warning/danger usan alert/assertive; info/success usan status/polite; `aria-atomic` queda activo por defecto. |
| F1.D OverlayFrame focus trap | Cerrado | `OverlayFrame.jsx` captura foco, hace loop Tab/Shift+Tab, cierra con Escape, recaptura `focusin`, restaura foco previo y oculta el backdrop de lectores con `aria-hidden`. |
| F1.E Checkbox + Radio | Cerrado | `Checkbox.jsx` y `Radio.jsx` existen como primitives nativos, exportados desde `ui/index.js`, con label/hint/error, `aria-describedby`, `aria-invalid`, disabled y estilos tokenizados. |
| F1.F IconButton | Cerrado | `IconButton.jsx` existe como primitive exportado, con label accesible, variantes, tamanos, estado active, disabled y loading con `aria-busy`. |
| F1.G SurfaceCard inverse | Cerrado | `SurfaceCard.jsx` soporta `tone="inverse"` y `tailwind.css` define `.sd-surface-tone-inverse` para superficies oscuras/inversas sin hacks locales. |
| F1.H ProgressSummary | Cerrado | `ProgressSummary.jsx` usa `data-sd-container="true"` y layout interno estable; el `@container (min-width: 54rem)` ya apunta a `.sd-progress-summary-layout`. |
| F1.H JourneyStepper | Cerrado | `JourneyStepper.jsx` usa `ol/li`, `aria-current="step"`, normalizacion de estados, clases tokenizadas y wrapper con `data-sd-container="true"`. |

## Trabajo fuera de alcance dejado vivo

Estos puntos siguen siendo relevantes, pero no pertenecian al cierre quirurgico de F1.next:

- Patrones de dominio: `QuestionPage`, `InfoPanel`, `AssessmentLayout`, `CheckAnswers`, `TaskList`, `ListDetail`, `SimulationChrome`.
- Layouts de dominio: `CourseLayout`, `LessonLayout`, `SimulationLayout`, `AdminLayout`.
- Refinar `SurveyView` para consumir `Checkbox`/`Radio` y un futuro `AssessmentLayout`.
- Refinar `CoursesView` y `LessonView`, que siguen fuera de alcance por regla explicita.
- Completar `signalActivities` / F6A.
- Cleanup de `app.css` / `legacy.css`.
- Drift de breakpoints JS/CSS y `DeviceShell` render-as-function, que corresponden a F2 refine.

## Archivos creados durante F1 closeout

- `frontend/src/components/ui/Checkbox.jsx`
- `frontend/src/components/ui/Radio.jsx`
- `frontend/src/components/ui/IconButton.jsx`
- `frontend/src/components/ui/SurfaceCard.stories.jsx`
- `docs/rebuild/audit/F1.I-closeout-final.md`

## Archivos modificados durante F1 closeout

- `frontend/src/design-system/tokens.js`
- `frontend/src/styles/tailwind.css`
- `frontend/src/components/ui/InlineMessage.jsx`
- `frontend/src/components/ui/OverlayFrame.jsx`
- `frontend/src/components/ui/SurfaceCard.jsx`
- `frontend/src/components/ui/index.js`
- `frontend/src/components/ui/Button.stories.jsx`
- `frontend/src/components/ui/FormPrimitives.stories.jsx`
- `frontend/src/components/ui/OverlayPrimitives.stories.jsx`
- `frontend/src/patterns/ProgressSummary.jsx`
- `frontend/src/patterns/JourneyStepper.jsx`
- `frontend/src/patterns/FoundationPatterns.stories.jsx`
- `test/foundation-smoke.test.js`
- `CLAUDE.md`
- `AGENTS.md`

## Validacion final

- `.\npm-local.cmd test` - PASS, 36/36 tests.
- `.\npm-local.cmd run build` - PASS, Vite build generado en `dist/`.
- `.\npm-local.cmd run build-storybook` - PASS, Storybook generado en `storybook-static/`.
- `git diff --check` - PASS, sin errores de whitespace. Solo avisos normales de LF -> CRLF en Markdown.

Nota: `build-storybook` mantiene el warning conocido de chunks mayores a 500 kB por Storybook/axe/iframe. No bloquea F1 closeout y no fue introducido por F1.I.

## Veredicto

F1 closeout queda cerrado. La foundation ya cubre los gaps obligatorios detectados en F0.9 y la validacion final de F1.I paso en verde.
