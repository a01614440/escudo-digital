# F6.G - Inbox / email refine

Fecha: 2026-04-23

## Objetivo

Cerrar la rama de correo de `InboxActivity` con identidad propia, mejor lectura y separacion clara respecto a SMS, sin tocar dominio ni renderer.

## Cambios realizados

- `frontend/src/components/activities/immersive/InboxActivity.jsx`
  - consolido la rama de email con `email-summary-shell` y banner mail-specific;
  - agrega vistas mail-specific para sidebar, lector, detalles, enlaces y review;
  - mantiene SMS como rama separada.
- `frontend/src/styles/app.css`
  - agrega estilos propios para el correo: superficie, banner, chips, lista, lector, cuerpo, enlaces, review y responsive/dark theme.
- `test/f6-email-refine-smoke.test.js`
  - protege la rama de correo y sus hooks CSS.

## Validacion

- `npm-local.cmd test`
- `npm-local.cmd run build`
- `git diff --check`

## Fuera de alcance

No se toco `ActivityRenderer.jsx`, `activityRegistry.js`, hooks de dominio, backend, services, contracts, scoring, `CoursesView.jsx`, `LessonView.jsx`, `app.css` fuera del bloque de inbox/email ni `legacy.css`.

## Estado

F6.G queda cerrada como refinamiento de inbox / email.
